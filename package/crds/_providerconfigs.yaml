---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: providerconfigs.
spec:
  group: ""
  names:
    kind: ProviderConfig
    listKind: ProviderConfigList
    plural: providerconfigs
    singular: providerconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    name: ""
    schema:
      openAPIV3Schema:
        description: ProviderConfig is the CRD type for Hostinger API provider configurations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProviderConfigSpec defines the desired state of a ProviderConfig.
            properties:
              apiKeyAuth:
                description: APIKeyAuth contains API v1 (API key) authentication credentials.
                properties:
                  apiKeySecretRef:
                    description: |-
                      APIKeySecretRef is a reference to a secret containing the API key.
                      The secret key should be "api-key".
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  customerIdSecretRef:
                    description: |-
                      CustomerIDSecretRef is a reference to a secret containing the customer ID.
                      The secret key should be "customer-id".
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  endpoint:
                    description: Endpoint is the Hostinger API v1 endpoint URL.
                    type: string
                required:
                - apiKeySecretRef
                - customerIdSecretRef
                - endpoint
                type: object
              oauthAuth:
                description: OAuthAuth contains API v2 (OAuth) authentication credentials.
                properties:
                  clientIdSecretRef:
                    description: |-
                      ClientIDSecretRef is a reference to a secret containing the OAuth client ID.
                      The secret key should be "client-id".
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  clientSecretSecretRef:
                    description: |-
                      ClientSecretSecretRef is a reference to a secret containing the OAuth client secret.
                      The secret key should be "client-secret".
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  endpoint:
                    description: Endpoint is the Hostinger API v2 endpoint URL.
                    type: string
                  tokenEndpoint:
                    description: TokenEndpoint is the OAuth token endpoint URL.
                    type: string
                required:
                - clientIdSecretRef
                - clientSecretSecretRef
                - endpoint
                - tokenEndpoint
                type: object
            type: object
          status:
            description: ProviderConfigStatus defines the observed state of a ProviderConfig.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              users:
                description: Users of this provider configuration.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
